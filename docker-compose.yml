version: '3.4'

services: 
  portainer:
    image: portainer/portainer-ce

  showroom.api:
    image: ${DOCKER_REGISTRY-}showroomapi
    build:
      context: .
      dockerfile: Services/Showroom/Showroom.Api/Dockerfile